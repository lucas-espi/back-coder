
<div>
    {{#if products}}
    <h1>Aqui tenemos la lsita de productos:</h1>

    <ul>
    {{#each products}}
        <li>
            <h4>Producto:{{this.title}}</h4>
            <p>Id:{{this.id}}</p>
            <p>Descripcion:{{this.description}}</p>
            <p>precio:{{this.price}}</p>
        </li>
    {{/each}}
    </ul>

    {{else}}
    <h2>Aun no tenemos productos cargados!</h2>
    {{/if}}
</div>

<style>
    body {
        font-family: Arial, Helvetica, sans-serif;
        background-color: #212F3C;
        padding: 3rem 3rem;
    }

    div {
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        background-color: #E8DAEF;
        border-radius: .6rem;
        color: #212F3C;
    }

    h1 {
        font-size: 2.5rem;
    }

    ul {
        list-style-type: none;
        padding: 0;
        display: flex;
        flex-wrap: wrap;
        justify-content: center;

    }

    li {
        margin: 1rem;
        background-color: #7daedd;
        border-radius: .4rem;
        padding: 1.2rem;
    }

    h4{
        text-align: center;
    }

    p {
        margin: 0;
        font-weight: 500;
        font-family: cursive;
    }

    h2 {
        color: #666666;
        font-size: 20px;
    }
</style>

<script src="/socket.io/socket.io.js"></script>
<script src="js/home.js"></script>

